# Пользователи и группы. Авторизацация и аутентификация.
__Бащовые понятия__   
 **AAA** 
- Authentication - аутентификация, идентификация, процесс подтверждения пользователем своей подлинности. Ввод логина и пароля.
- Authorization - авторизация, процесс наделения пользователя правами (предоставления доступа к каким-либо объектам)
- Accounting - запись информации о произошедших событиях    

- Условия для входа
   - shell должен существовать и быть перечисленным в /etc/shells
   - установлен пароль
- Блокировка пользователя:
   - установка shell в /bin/nologin
   - установка expiry date в прошлое
   - Блокировка паролы (делаем его непроверяемым, добавляя "!")
- Файл настроек для управления политиками при создании пользователей:     
/etc/login.defs



__Классическая аутентификация__     
1. /etc/passwd - список пользователей   
- Имя пользователя
- Цифровой идентификатор пользователя (UID)
- Цифровой идентификатор основной группы пользователя (GID)
- Полное имя пользователя
- Домашний каталог пользователя
- Оболочка входа в систему
2. /etc/group - группы пользователей
- Имя группы
- Пароль группы
- Цифровой идентификатор группы (GID)
- Список пользователей, входящих в эту группу, как в дополнительную
- Основная группа пользователя - в /etc/passwd
3. /etc/shadow - пароли пользователей
- Регистрационное имя
- Захэшифрованный пароль
- Дата последнего изменения пароля
- Минимальное число дней между изменениями пароля
- Максимальное число дней между изменениями пароля
- Количество дней до истечения срока действия пароля
- Количество дней по истечении срока действия пароля
- Срок действия учетной записи
- Зарезервированное поле, которое в настоящее время всегда пустое


**Типы пользователей в Linux**
1. Суперпользователь root (UID = 0)
- Home /root
- Пароль не всегда
- Есть shell
- Неограниченные полномочия
2. Обычный пользователь user (UID > 1000)
- Home /home/user
- Пароль установлен
- Есть shell
- Доступ в рамках домашней директории
3. Системный пользователь daemon (UID < 1000)
- Home Любая
- Паролā нет
- Нет shell
- Ограниченный доступ

__Управление пользователями__    
- useradd - создание пользователя
- adduser - создание пользователя (скрипт)
- usermod - изменение пользователя
- userdel - удаление пользователя
- passwd - изменение пароля
- chage - изменение свойств пароля
- groupadd - создание группы
- groupdel - удаление группы
- id - просмотр свойств пользователя

__Механизм sudo__    
- su — переключение пользователя
- sudo su — переключение на суперпользователя (root)
- sudo — выполнение команды с правами суперпользователя
- /etc/sudoers — конфигурация sudo

__Права доступа к файлам__   

<p align="center">
<image src="https://github.com/LLlMEJIb87/LINUX/blob/main/%D0%91%D0%B5%D0%B7%D0%BE%D0%BF%D0%B0%D1%81%D0%BD%D0%BE%D1%81%D1%82%D1%8C/Pictures/bits_dostup.PNG">
</p>    
  
 1. r - readable (чтение) - цифровое обозначение 4
- файл чтение файла   
- директория просмотр списка имен    
2. w - writeable (запись) - цифровое обозначение 2
- файл изменение файла    
- директория изменение объектов директории   
3. x - executeable (исполнение) - цифровое обозначение 1
- файл исполнение файла    
- директория доступ к inode в директории (вход, поиск)

 
-обычный файл    
l - символическая ссылка    
d - директория   
b - блочное устройтсво    
с - символьное устройство    
p - именнованный канал    
s - UNIX-сокет   

<p align="center">
<image src="https://github.com/LLlMEJIb87/LINUX/blob/main/%D0%91%D0%B5%D0%B7%D0%BE%D0%BF%D0%B0%D1%81%D0%BD%D0%BE%D1%81%D1%82%D1%8C/Pictures/spec_bits_dostup.PNG">
</p>        

https://www.gnu.org/software/coreutils/manual/html_node/Changing-Special-Mode-Bits.html    

__Расширенные атрибуты файлов___   

 Атрибуты
- a - файл может быть открыт только в режиме добавлениā
- A - не обновлять время перезаписи
- c - автоматически сжимать при записи на диск
- C - отключть копирование при записи
- i - сделать неизменяемым (даже для root)
- s - безопасное удаление с последующей перезаписью нулями    
```
 Изменение: chattr +i test_file
 Просмотр: lsattr
```

## Access Control List  

<p align="center">
<image src="https://github.com/LLlMEJIb87/LINUX/blob/main/%D0%91%D0%B5%D0%B7%D0%BE%D0%BF%D0%B0%D1%81%D0%BD%D0%BE%D1%81%D1%82%D1%8C/Pictures/ACL.PNG">
</p>      

- Повышение гибкости назначения прав доступа
- Поддержка на уровне файловой системы
- Может потребовать установки утилит
```
apt instal acl
```
- tune2fs -l /dev/mapper/ubuntu--vg-ubuntu--lv | grep acl
- setfacl -m u:zabbix:r test_file - установить ACL
- setfacl -m d:u:vagrant:rwx -R /usr/local/upload/ - установить дефолтные ACL (только для директорий)
- getfacl test_file - показать ACL
- setfacl -x u:zabbix FILE - убрать ACL

## Capabilitties
Механизм capability (возможности, полномочия) в Linux — это способ гранулировать права суперпользователя (root), чтобы процессам не нужно было иметь полный root-доступ, а только те привилегии, которые им реально нужны. Это повышает безопасность системы.   

__Что было раньше__    
До появления capability в Linux действовало простое правило:
- Если процесс UID = 0 (то есть root) → он может всё.
- Если не root → почти ничего из системных операций.        

__Что изменилось с появлением capabilities__     
Сейчас права root разбиты на отдельные флаги — capabilities, и процесс может получить только нужные ему привилегии.     

Например:

- CAP_NET_ADMIN — управлять сетевыми интерфейсами
- CAP_SYS_BOOT — перезагружать систему
- CAP_CHOWN — менять владельца файлов

И так далее. Всего ~40 capability.     

__Пример: ping без root__     
Раньше ping требовал root, потому что использует raw sockets. Теперь можно:
```
sudo setcap cap_net_raw+p /bin/ping
```
И всё — ping можно запускать обычному пользователю, но он не получит остальных root-привилегий.     

### Виды capability-наборов
У каждого процесса есть наборы capability:
- Permitted	Что процесс может использовать
- Effective	Что процесс реально использует в данный момент
- Inheritable	Что можно передать дочерним процессам
- Bounding	Максимально возможные привилегии
- Ambient	Возможности, сохраняющиеся при запуске не-привилегированного бинаря


### Утилиты для работы

- getcap <файл> — показать capability файла
- setcap cap_net_bind_service=+ep <файл> — дать возможность слушать порты <1024
- capsh --print — показать capability текущего процесса
- pscap <PID> — посмотреть capability процесса (из пакета libcap2-bin)

Установка
```
sudo apt install libcap2-bin
```

### Таблица Capability

## 🧾 Таблица Linux Capabilities

| Capability              | Назначение                                                       | Примеры использования                         |
|-------------------------|------------------------------------------------------------------|-----------------------------------------------|
| **CAP_CHOWN**           | Менять владельца файлов                                          | `chown`, FTP-сервер                           |
| **CAP_DAC_OVERRIDE**    | Игнорировать права доступа (`read/write/exec`)                   | Чтение чужих файлов                           |
| **CAP_DAC_READ_SEARCH** | Игнорировать только `read` и `search` (не `write`)               | Backup-софт, индексаторы                      |
| **CAP_FOWNER**          | Игнорировать ограничения владельца файлов                        | Удаление чужих сокетов                        |
| **CAP_FSETID**          | Сохранять setuid/setgid биты при изменении                      | Установка бинарей с setuid                    |
| **CAP_KILL**            | Посылать сигналы чужим процессам                                | `kill`, system monitors                       |
| **CAP_SETGID**          | Менять group ID процесса                                        | `setgid`, PAM                                 |
| **CAP_SETUID**          | Менять user ID процесса                                         | `setuid`, PAM                                 |
| **CAP_SETPCAP**         | Изменять capability у процесса                                  | Контроль прав в runtime                       |
| **CAP_LINUX_IMMUTABLE** | Менять флаг `immutable` файлов                                   | Безопасность файлов (`chattr +i`)             |
| **CAP_NET_BIND_SERVICE**| Слушать порты <1024                                             | Веб-серверы (HTTP, HTTPS, DNS)               |
| **CAP_NET_BROADCAST**   | Отправка широковещательных пакетов                              | Старые сетевые утилиты                        |
| **CAP_NET_ADMIN**       | Управление сетевыми интерфейсами                                | `ip link`, `tc`, `brctl`, VPN                 |
| **CAP_NET_RAW**         | Использование raw sockets                                       | `ping`, `traceroute`                          |
| **CAP_IPC_LOCK**        | Блокировка памяти в RAM (mlock)                                 | Безопасность, OpenSSL                         |
| **CAP_IPC_OWNER**       | Игнорировать права на IPC объекты                               | Очереди, семафоры                             |
| **CAP_SYS_MODULE**      | Загружать и выгружать модули ядра                               | `modprobe`, `insmod`                          |
| **CAP_SYS_RAWIO**       | Доступ к `ioctl`, `ioperm`, `iopl`                              | Работа с железом                              |
| **CAP_SYS_CHROOT**      | Использование `chroot()`                                        | Sandboxing, контейнеры                        |
| **CAP_SYS_PTRACE**      | Трассировка других процессов                                    | `gdb`, `strace`, `perf`                       |
| **CAP_SYS_PACCT**       | Учет процессора (`acct()`)                                      | Мониторинг                                    |
| **CAP_SYS_ADMIN**       | ⚠️ Очень много: mount, swap, loop, sysctl и др.                | LVM, Docker, systemd                          |
| **CAP_SYS_BOOT**        | Перезагрузка, `reboot()`                                        | System manager, watchdog                      |
| **CAP_SYS_NICE**        | Менять приоритеты (nice/rt)                                     | Realtime процессы                             |
| **CAP_SYS_RESOURCE**    | Изменять resource limits (`ulimit`)                             | `prlimit`, ресурсоемкие процессы              |
| **CAP_SYS_TIME**        | Установка времени                                               | `date`, NTP                                   |
| **CAP_SYS_TTY_CONFIG**  | Настройка tty устройств                                         | `stty`, серийные консоли                      |
| **CAP_MKNOD**           | Создание спец. файлов (device nodes)                            | `mknod`, udev                                 |
| **CAP_LEASE**           | Установка file leases                                           | NFS, блокировки                               |
| **CAP_AUDIT_WRITE**     | Запись в audit-лог                                               | `auditd`                                      |
| **CAP_AUDIT_CONTROL**   | Настройка audit                                                 | `auditctl`                                    |
| **CAP_SETFCAP**         | Установка файловых capability                                   | `setcap`                                      |
| **CAP_MAC_OVERRIDE**    | Игнорирование MAC-политик (например, SELinux)                  | Утилиты админа                                |
| **CAP_MAC_ADMIN**       | Управление MAC-политиками                                      | SELinux, AppArmor                             |
| **CAP_SYSLOG**          | Чтение/настройка ядрового журнала                              | `dmesg`, `syslog`                             |
| **CAP_WAKE_ALARM**      | Установка таймеров пробуждения                                 | RTC, ноутбуки                                 |
| **CAP_BLOCK_SUSPEND**   | Блокировка перехода в suspend                                   | `pm-utils`, фоновые процессы                  |
| **CAP_AUDIT_READ**      | Чтение audit-логов ядра                                        | Системный аудит                               |


## Polkit
Polkit (ранее PolicyKit) — это один из ключевых механизмов в Linux для контроля доступа к привилегированным операциям, особенно в системах, где работают GUI/desktop-программы или службы, которые запускаются от root.    

🧠 __Что такое Polkit__

**Polkit** — это компонент **контроля прав доступа**, который определяет:

> **Можно ли НЕ-root-пользователю выполнять привилегированное действие**, и если да, **на каких условиях**.

Он **не заменяет `sudo`**, а **дополняет его**, особенно в контексте **служб systemd**, **DBus**, **desktop-утилит**, `udisks`, `packagekit`, `network-manager` и т.п.    

 🧩 __Как это работает__

1. Программа делает **запрос к привилегированной службе** (например, через DBus).

2. Служба вызывает **Polkit** с вопросом:  
   ➜ "Разрешено ли пользователю `dmitriy` выполнить **вот такую** операцию?"

3. Polkit анализирует:
   - Кто запрашивает
   - Какое действие
   - Что говорят политики
   - Может — запросить пароль

4. Polkit возвращает: _yes_, _no_ или _authentication required_.

 📖 __Примеры действий (actions)__

Polkit не работает напрямую с командами — он оперирует **"действиями" (actions)**.  
Каждая служба (udisks, NetworkManager и др.) описывает, какие действия она поддерживает.

Примеры:

| Action ID                                                 | Описание                                |
|-----------------------------------------------------------|-----------------------------------------|
| `org.freedesktop.login1.reboot`                           | Перезагрузка                            |
| `org.freedesktop.systemd1.manage-units`                   | Управление systemd unit'ами             |
| `org.freedesktop.udisks2.filesystem-mount`                | Монтирование файловой системы           |
| `org.freedesktop.network-manager.settings.modify.system`  | Изменение системных сетевых настроек    |    

 🔐 __Уровни прав доступа__

Политики могут задавать, как именно будет разрешаться доступ к действию:

- `yes` — всегда разрешено
- `auth_self` — пользователь может ввести **свой** пароль
- `auth_admin` — требуется быть в группе админов (`wheel`, `sudo`) и ввести **админский** пароль
- `no` — всегда запрещено    

 🚨 __Важно__

- Polkit **НЕ ЗАЩИЩАЕТ** систему от эксплойтов — он только **контролирует доступ на уровне логики**.
- В системах **без desktop-среды** он почти не используется.
- На серверах чаще используют **`sudo`**, а не Polkit.

## Pluggable Authentication Modules (PAM)    

PAM (Pluggable Authentication Modules) — это ядро всей системы аутентификации в Linux. Если ты когда-нибудь логинился в систему, вводил sudo, подключался по SSH, — ты взаимодействовал с PAM, даже если не знал об этом.    


 🔐 __Что такое PAM__

**PAM** (Pluggable Authentication Modules) — это **модульная система аутентификации**, которая позволяет Linux и другим Unix-системам **гибко управлять процессом входа, проверки паролей, доступа и сессий**.  
Она работает как **прослойка между сервисом и механизмами проверки прав**.

> **Просто говоря:** PAM говорит: "Ты хочешь залогиниться? Хорошо, я проверю твой пароль, могу попросить отпечаток пальца, проверить, не заблокирован ли ты, и только потом пущу".

 🧱 __Из чего состоит PAM__

PAM состоит из **четырёх типов модулей**, которые могут использоваться в любом сочетании:

| Тип модуля     | Назначение                                                         |
|----------------|---------------------------------------------------------------------|
| `auth`         | Аутентификация (пароль, отпечаток, двухфакторка и т.д.)             |
| `account`      | Проверка политики учётной записи (заблокирована ли, права и т.д.)   |
| `password`     | Обновление пароля (смена, политика сложности и т.п.)                |
| `session`      | Сессия пользователя (монтирование директорий, запуск скриптов)      |     

 📁 __Где находятся конфиги PAM__

Конфигурация PAM находится тут:

- Основные файлы:  
  `/etc/pam.conf` — **глобальный** конфиг (редко используется)
- Каталог с настройками для каждого сервиса:  
  `/etc/pam.d/`

Примеры файлов:

- `/etc/pam.d/login` — вход в систему
- `/etc/pam.d/sshd` — вход по SSH
- `/etc/pam.d/sudo` — проверка при использовании sudo
- `/etc/pam.d/common-auth`, `common-password`, `common-session`, `common-account` — общие настройки, подключаются из других


